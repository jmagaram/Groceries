@inherits LayoutComponentBase
@using Models
@using static Models.StateExtensions

@if (StateService.CurrentState.LoggedInUserSettings().FontSize.IsLargeFontSize) {
    <style>
        body {
            font-size:larger;
        }
    </style>
}
<div class="container-fluid px-0">
    @Body
</div>

@code {
    [Inject]
    public Service StateService { get; set; }
}